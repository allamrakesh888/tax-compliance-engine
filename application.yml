spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/task_manager
    username: rakesh
    password: 
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      #ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true # Optional: Makes the logged SQL pretty and readable